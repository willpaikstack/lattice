# Lattice OS App Feature Map

Last updated: 2026-08-18

This is the living map of what exists inside Lattice OS, how each area works, what data powers it, and what is still prototype or future work. Update this file whenever a feature is added, removed, renamed, moved to a new route, connected to a new data source, or promoted from static/prototype behavior to operational behavior.

## Status Key

- `Operational` - uses real app workflow data and is expected to work in local development when required services are configured.
- `Partial` - visible and useful, but still missing durable persistence, production storage, ownership checks, external service configuration, or other hardening.
- `Prototype` - useful as a design/product reference, but still partly static, local-only, or not the final workflow.
- `Placeholder` - route or module exists, but the feature is intentionally future-facing.

## Product Model

Lattice OS is a manufacturing RFQ, procurement, and supplier-network operating system. The app is organized around four spaces:

- Public access-request intake for prospects.
- Customer workspace for RFQs, quotes, orders, resources, and notifications.
- Admin workspace for internal RFQ review, quote issuance, customer/vendor management, orders, and documents.
- Supplier workspace for supplier order visibility and invoice access.

The current source-of-truth workflow is `Request`/`LatticeRequest`: a buyer submits an RFQ, Lattice reviews it, admin issues a quote, the buyer accepts or checks out, and the purchased request becomes an order.

## Public And Guest Features

| Feature | Routes | What It Does | Data Source | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Manufacturing proof landing page | `/` | Positions Lattice as qualified global CNC machining and fabrication capacity for domestic machine shops managing overflow and cyclical demand, and previews the managed RFQ, inspection, and production-update model. | Static public page, generated manufacturing imagery, and documented supplier capability sources. | Operational | Responsive desktop/mobile presentation; the hero emphasizes protecting scheduled commitments, lead times, and customer responsiveness, with a quiet `See how Lattice delivers` link to the service explainer. The partner-network experience section identifies source-backed industry coverage without customer logos, names, or implied endorsement. The quality section explains that requested inspection reports and material documentation are uploaded to Lattice for customer review before shipment. Capabilities, Materials, and Quality remain account-gated workspace resources. The header's primary action is `Request an account` to `/waiting-list`. |
| How Lattice works | `/how-it-works` | Account-safe public one-pager positioning Lattice as managed qualified manufacturing capacity: customers submit a complete manufacturing package, Lattice validates the production path and issues a supplier-backed quote, production launches on approval, and required quality documentation is attached for review before shipment. | Static public page with client-side benefit disclosures, network statistics derived from `src/lib/vendor-equipment.ts`, researched supplier/material qualification evidence, and a generated regional network illustration with code-positioned city markers. | Operational | The header’s `How it works` link is the only reactivated informational navigation item. It routes to this standalone public explainer, outside the authenticated workspace shell, and sends access requests to the existing `/waiting-list` flow; it does not offer guest RFQ submission. The problem stories and the solution/first-job guidance use matching two-column desktop treatments while retaining their original stacked reading order on smaller screens. First-job guidance precedes a four-column desktop workflow connected by a subtle directional path; narrower screens retain a readable stacked step sequence. The value area includes a China map marking the operator-confirmed network cities in Shenzhen, Dongguan, Beijing, Shanghai, and Tianjin without displaying a public city legend; supplier identities and exact facility addresses remain confidential. A detailed desktop page index docks to the right viewport edge, expands inward on hover or keyboard focus, and includes a direct Network reach anchor. Four accessible expandable benefit rows keep one detail panel open at a time. The capacity row summarizes the dynamically derived CNC machine total, process mix, maximum documented envelopes, a 400+ combined partner-network workforce, and a 17-CMM aggregate with supporting vision, material-analysis, surface, hardness, ultrasonic, and traceability controls. The capacity-on-demand row explains job-by-job access to qualified CNC capacity, flexible scaling with backlog, and the absence of a long-term volume or fixed-infrastructure commitment. The managed-quality row explains a four-stage supplier-vetting and job-quality model—vet the supplier, qualify the job, control production, and verify before shipment—then uses customer-requested certifications to shortlist eligible vendors and matches job requirements to documented inspection capacity and traceability controls before award. Certificate holder, scope, and currency are confirmed before release. Benefits without defensible tracked outcome data remain unnumbered. The page ends with a customer-origin credibility note plus an invite-only platform-access request CTA. Its header retains login access and uses the same `Request access` CTA label at every breakpoint. |
| Login | `/login`, `/login/sso-callback`, `/sign-in`, `/sign-up` | Uses Clerk's hosted sign-in/sign-up experience with Google and password options configured in Clerk; Lattice reads the authenticated Clerk identity and maps it to a provisioned company/role membership. | Clerk Production/Development keys by environment; Prisma `User`/`Company` membership records and `User.clerkUserId` identity link. | Partial | The optional catch-all login route keeps Clerk path-based SSO callback and error states inside the login surface rather than falling through to the authenticated-shell 404. On each sign-in, Clerk’s display name synchronizes to the matching Lattice user record; the dashboard and sidebar account card also use that Clerk name immediately while the Lattice profile link is pending. Only existing/provisioned Lattice users receive a workspace session; a Clerk-only identity is not auto-authorized. Existing database users must be linked by matching email on first sign-in or through the one-time `migrate:clerk-users` operator script. Google is configured in Clerk Production with custom credentials; the production Prisma schema must be applied before cutover. |
| Password recovery and temporary-password completion | `/forgot-password`, `/reset-password`, `/account/set-password` | Sends a generic recovery confirmation, issues a short-lived one-time reset token only for provisioned users, and lets users choose a new password. Administrator-generated temporary passwords must be replaced before workspace access. | Prisma `PasswordResetToken`, `AuthAuditEvent`, and `User` password state; Resend or local development outbox delivery. | Partial | Recovery links are single-use and expire after one hour. Temporary passwords expire after 72 hours; the completion route refreshes the signed session after a successful personal-password change. Production requires Resend/domain environment configuration and the Prisma schema to be deployed. |
| Waiting list | `/waiting-list` | Captures invite-only platform access requests from domestic manufacturers, including the shop, requester, and overflow or out-of-capability work they want supported; blocks exact duplicate emails and routes company-domain duplicates to the existing contact. | Prisma/Neon when available, local fallback store in development, Resend/local outbox for email. | Operational | The public experience now matches the current light Lattice marketing system, explains qualified capacity, managed execution, and pre-shipment quality evidence before asking for contact details, provides pending submit feedback, and includes dedicated duplicate-request and confirmation states. Production email sending still depends on Resend/domain setup. |

## Customer Workspace

| Feature | Routes | What It Does | Data Source | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Customer shell | Customer routes under `/dashboard`, `/requests`, `/quotes`, `/orders`, `/materials`, `/equipment`, `/quality-documentation`, `/notifications`, `/account/settings` | Provides the buyer-facing app frame, sidebar, role-aware admin bridge, customer navigation, Notion-style notification activity view on bell click, and profile-card account access. | Session role, app-shell components, and `/api/customer-notifications` for the sidebar preview. | Operational | The shared header brand uses the official outer-diamond, four-cell Lattice mark and uppercase `LATTICE` wordmark. The roadmap is intentionally hidden from customer navigation and customer-role route access until it is reintroduced. The `Your Resources` navigation includes `Inspection & Certificates`, linking to the customer quality documentation guide. The profile card opens account settings through its own link, while the separate account-menu button reliably exposes account actions and dismisses on outside click. Admin sessions can operate customer routes for development/support simulation. The bell keeps the desktop sidebar header/ribbon stable while swapping the content below it; mobile opens an overlay preview. No unread badge is shown until durable read state exists. |
| Dashboard command center | `/dashboard` | Leads with four operational KPI cards, followed by the workflow-based Action Center and quote/order activity. | `listBuyerQuotes()`, `listBuyerOrders()`, `buildCustomerDashboardSummary()`, `buildCustomerActionWorkflows()`, company-scoped customer ownership filter. | Operational | KPI detail copy keeps active orders concise and treats `SHIPPED` supplier status as orders in transit. Notification history lives only in `/notifications`; values are derived from existing RFQ/order records and filtered to the signed-in customer's company unless the session is admin. |
| Customer Action Center | `/dashboard#action-center` | Shows a single summary sentence and compact workflow rows with a state marker, reference, concise status, and continuation action. | `buildCustomerActionWorkflows()` in `src/lib/customer-action-center.ts`, using existing RFQ/quote/order state. | Operational | Customer-owned actions and overdue items retain stronger state markers. Purchased orders without a supplier or quote milestone date use a neutral monitoring state because no customer action is required. Detailed workflow steps remain available on the order or quote record rather than in the dashboard. |
| Notification center | `/notifications`, `/api/customer-notifications` | Shows the full chronological customer activity history, including action-related events, lower-level RFQ history, operator updates, and customer-facing request-info/no-quote notes. The global bell previews newest derived notifications below the stable sidebar header, grouped as This week, Last week, and Older with dividers between rows. | `buildCustomerActivityFeed()` plus open workflow count from `buildCustomerActionWorkflows()`; sidebar preview returns latest derived rows with raw timestamps through `/api/customer-notifications`. | Operational | `/notifications` remains the full in-app notification history. This-week rows use compact relative age labels such as `2d`; older groups show compact dates. Reading a notification does not resolve an action workflow. V1 has no persistent read/unread state. Working spec: `docs/customer-inbox-notification-spec.md`. |
| New RFQ request | `/requests/new` | Leads with CAD upload, a visible Upload/Configure/Review/Submit progression, autosave reassurance, and a continuation list limited to genuine unsubmitted drafts; uploaded files then unlock the existing multi-line-item material/process configuration, Hubs-aligned material metadata, ISO 2768-1 tolerance reference popup, surface finish secondary choices, inspection/documentation choices, drawing-required validation, ship-to/requester snapshots, draft recovery, and reorder flow. | Request form, CNC material library, RFQ option metadata, local draft upload storage, request API, Prisma/local fallback, company-scoped ownership filter for reorder/draft prefill. | Operational | Standard Inspection is always included and cannot be removed. Optional quality choices are dimensional, formal dimensional, CMM, FAIR AS9102, custom inspection, and MTR; source inspection and build-and-hold first article were removed from the customer selector. The quality section links to `/quality-documentation`, which defines each document and explains CMM versus a dimensional report. Custom Inspection prompts the buyer to describe the scope in Manufacturing notes. Draft rows include Continue and Archive actions. Archive prompts for an optional reason, hides the draft from continuation, uses `Request.isArchived` for saved drafts, and removes browser-local drafts from local incomplete RFQ storage. Browser-local draft data is read after hydration so the initial server and client renders remain deterministic. Submitted RFQs no longer appear under draft continuation. Material dropdown options show UNS numbers and compact composition formulas where applicable. Tolerances include a compact Hubs-style ISO 2768-1 standards modal. The technical-drawing PDF preview remains mounted while its tolerance and drawing-required selections change; clicking its dimmed backdrop uses the same validated close behavior as Done. Configured line items can be deleted from the line-item header after a confirmation modal. Surface finishes expose only the relevant cosmetic requirement, color, or RAL/Pantone fields for each selected finish. Production file storage still needs R2/S3 replacement for local `.data/uploads`. |
| Quality documentation guide | `/quality-documentation` | Defines the customer-facing inspection and certificate choices in plain language, including the difference between a dimensional report and CMM inspection. | Static route content linked from the RFQ form and the ZEISS equipment records in `src/lib/vendor-equipment.ts`. | Operational | Uses a single-column, Notion-inspired documentation layout with breadcrumbs, page metadata, inline sections, and a desktop contents tab that docks to the right edge and expands on hover or keyboard focus. Larger production lots use 100% inspection for critical dimensions and an RFQ-confirmed ISO 2859-1 / ANSI-ASQ Z1.4 sample plan for non-critical dimensions. CMM inspection references the calibrated ZEISS CONTURA and ACCURA equipment capacity; per-part uncertainty remains inspection-plan specific. Custom Inspection scope is defined in Manufacturing notes; drawing-required options are called out in the guide. |
| Buyer quotes list | `/quotes` | Shows active RFQs and quote-received records, excluding purchased quotes, sorted by most recently edited and paginated three rows at a time per section. Browser-only drafts use a local `Discard draft` action; saved RFQs use the server-backed delete action. | Buyer quote repository, lifecycle helper, company-scoped ownership filter, browser draft storage. | Operational | Purchased records live in Orders. Customer sessions see records owned by their company; admins see all for support. Browser-only draft and deletion state loads after hydration to keep the initial render deterministic. Draft discard does not make a server delete request. |
| Buyer quote detail | `/quotes/[requestId]` | Shows a status/next-step summary, a quote-sheet-style Parts and pricing table, quote activity, CAD thumbnails, quote PDF, purchase actions, and customer-facing operator notes for missing-info or no-quote outcomes. | Request record, customer quote versions, uploaded files, APS preview URNs, company-scoped ownership policy. | Operational | Parts and pricing aligns Part / File, Specifications, Qty, Unit price, Line total, Lead time, and subtotal on desktop, scrolls horizontally inside the card when the desktop main column is constrained, then stacks before the mobile layout becomes too narrow. It does not force horizontal page scrolling. Purchased quote detail redirects to order detail. Unauthorized customer direct URLs return not found. `NEEDS_INFO` reads as a clarification request; `CLOSED` with an operator note reads as `No quote` / unable to quote. Buyer quote modification is intentionally deferred for now. |
| Reorder | `/requests/new?reorder=[requestId]` | Starts a new RFQ draft from a previous purchased order. | Existing order snapshot and draft request form. | Partial | Good buyer convenience flow; lineage views can still improve. |
| Quote checkout | `/quotes/[requestId]/checkout` | Collects delivery/import details, payment or purchase-order information, and converts a quote to a purchased order after valid payment/PO checkout. | Stripe PaymentIntent/CardElement, purchase payment snapshot, customer PO attachment records, company-scoped ownership policy. | Partial | Production Stripe env vars/webhook are required for live reliability. Server actions also enforce ownership before recording payment state or saving PO files. |
| Stripe return pages | `/quotes/[requestId]/stripe/success`, `/quotes/[requestId]/stripe/cancel` | Handles card payment return/cancel paths. | Stripe checkout/payment state. | Partial | Inline card checkout is primary; hosted redirect remains fallback. |
| Buyer orders list | `/orders` | Shows purchased orders with the shared order status, next milestone, and overdue milestone warning. | Buyer order repository filtered from purchased requests, company-scoped ownership filter, `order-progress` helpers. | Operational | Active post-purchase home for accepted quotes. Past expected milestones are visibly late. |
| Buyer order detail | `/orders/[requestId]` | Leads with one operational order-health panel covering shared status, latest customer update, next milestone/owner, overdue state, and tracking; supporting shipment, part, invoice, and document detail follows below. | Purchased request/order data, supplier order fields, tracking number helper, `order-progress` helpers, invoice renderer, company-scoped ownership policy. | Operational | Customer invoice/help/reorder actions are consolidated in the summary rail instead of duplicated in the page header. Unauthorized customer direct URLs return not found. Durable issued invoice records remain future accounting hardening. Package tracking links out to the detected carrier or a carrier search in V1. |
| Buyer order help | `/orders/[requestId]/help` | Provides an order-specific help/support route. | Order context. | Partial | Support workflow can be made more operational later. |
| Buyer invoice PDF | `/orders/[requestId]/invoice.pdf` | Renders order-specific customer invoice PDF. | Order-backed invoice renderer. | Operational | Uses repeatable order-derived invoice references; saved invoice issuance is future work. |
| Shipped orders | `/shipped` | Shows shipped-order tracking and tracking-number coverage. | Purchased orders with supplier status `SHIPPED`. | Operational | `Delivered` remains reserved until a durable delivery confirmation exists. |
| Account settings | `/account/settings` | Lets customer account defaults such as company and contact settings be managed. | Server account defaults with post-hydration local fallback. | Partial | Customer work is now company-owned; customer membership-management UI remains future work. The page provides a descriptive heading, a named profile-photo upload, keyboard-operable tabs with labelled panels, visible keyboard focus, and announced status and validation feedback. |
| Product roadmap | `/roadmap` | Retained roadmap implementation for later reintroduction. | Static roadmap item repository plus `RoadmapInterest` Prisma records with local `.data/roadmap-interests.json` fallback in development. | Deferred | Hidden from customer navigation and customer-role access; admins retain access for internal review. |
| Materials catalog and unlisted-material inquiry | `/materials`, `/materials/[slug]`, `/materials/inquiry`, `/materials/design-directions` | Presents the network's material coverage as a family-first atlas without search. Each full card opens a dedicated family guide with a visual overview, common-grade selection profiles, and the complete grouped grade catalog. Materials outside the atlas route to a structured inquiry form instead of the CAD-first RFQ. | `src/lib/customer-material-catalog.ts`, `src/lib/material-family-view-models.ts`, `src/lib/material-grade-properties.ts`, `src/components/material-grade-directory.tsx`, `src/components/plastic-functional-profile.tsx`, `src/components/plastic-functional-profile-group.tsx`, `src/lib/cnc-material-library.ts`, `src/lib/material-inquiries.ts`, Prisma `MaterialInquiry` records with `.data/material-inquiries.json` development fallback, [mechanical-property source log](material-mechanical-property-sources.md), [mechanical-property coverage](material-mechanical-property-coverage.md), [machinability coverage](material-machinability-coverage.md), generated imagery and swatches in `public/materials/`, and registered vendor sources in `docs/vendor-sources/`. | Operational | Customers browse all families, open any family guide, compare common grades by form/use, machinability, and selection note, browse all grades, and use the shared workspace `Request Quote` action when ready to start an RFQ; material catalog and guides do not repeat request buttons. All material pages inherit the workspace canvas color rather than adding a separate page-tinted panel. The unlisted-material form captures designation/specification, company, stock form, quantity, application requirements, and notes, explains the validation workflow, and confirms submission. Aluminum is presented as 21 alloy offerings, each revealing supported tempers rather than duplicate peer rows; all now resolve to a source-linked property row. Where the vendor listing omits temper, the directory labels a cited reference condition rather than treating it as supplied stock. Canonical rows and grade counts collapse documented aliases such as `SS 304` and `Alloy 625` while keeping distinct conditions traceable. Standard 304/304L and 316/316L stainless are each presented as a single dual-certified offering; LVM, titanium-stabilized, and high-temperature variants remain distinct where their specification matters. The 303Se and 303Sulf free-machining variants sit in the 300-series austenitic group; generic family labels such as `SS 300 series` are omitted from customer and RFQ material selection. Verified UNS designations are shown where a material has one, while proprietary product names such as MIC-6 are explicitly marked as having no standard UNS. Standard wrought Aluminum offerings are grouped by their numbered alloy series, with cast/tooling material separated only where applicable. Plastic common and directory grades use a shared native `Behavior details` disclosure, anchored beneath the matching machinability rating rather than an always-visible trait rail or mechanical-property subcard; only one polymer behavior panel remains open at a time. Its flush 2 × 2 expanded state shows heat tolerance, moisture response, chemical resistance, and wear / friction; exact resin data sheets are confirmed during RFQ review. Unresolved values remain visibly blank or `Not published`. Vendor provenance stays internal; supplier availability, certification, and traceability are confirmed through inquiry/RFQ review. |

### Catalog terminology

- Stainless offerings that require precipitation hardening are grouped under **Precipitation-hardened stainless steels**. This describes the material family, rather than naming the treatment process.
| Capabilities catalog | `/capabilities` | Customer-workspace overview of CNC machining, fabrication, supporting-process, inspection coverage, and aggregate network scale. | Workspace-safe capability copy, curated factory-floor images in `public/capabilities/factory-proof/`, and [network-scale source notes](vendor-sources/network-scale.md). | Operational | Uses production-environment photography without naming a supplier or promising a particular facility, availability, or fit for a job. The inspection-ready card uses a dedicated CMM-laboratory scene. The capability list ends with the source-backed aggregate of 590+ people and 42,000 m² of production space across the named manufacturing partners in Lattice's network. The factory thumbnails update the hero photo for the current visit and reset to the default on page load. It remains inside the authenticated app shell and keeps the resource navigation ribbon visible. |
| Equipment catalog | `/equipment` | Helps buyers compare known machine models, available sets, supplier-reported positional accuracy, work envelope, and available specifications. | Customer-safe fields from `src/lib/vendor-equipment.ts`, internal registered sources in `docs/vendor-sources/`, and optional record-level image metadata. | Prototype | The active catalog is a compact, searchable comparison table grouped by customer-visible process category: CNC Milling, CNC Lathe, QC & Inspection, Manual Machines, Sheet Metal, and EDM. It includes the normalized 2026-04-14 Jucheng equipment inventory (32 added records across CNC/EDM, sheet metal, manual, and inspection capacity). Source-backed Finishing, Die Casting, and Additive Manufacturing records are retained internally but intentionally hidden. Buyers can combine process, make/model search, and a direct-select equipment-type rail (with consolidated CNC Mill, Lathes, Manual equipment, QC equipment, and Sheet metal fabrication filters). The selected type is indicated by an underline and each option shows its matching model count; the rail suppresses unnecessary scrollbars while retaining horizontal overflow on narrow screens. `CNC Mill` is category-scoped, so axis-count language on Swiss-type lathes cannot leak them into milling results. Each grouped subtable owns its matching column schema: Manual Machines, QC & Inspection, and Sheet Metal show make/model, sets, and specifications; the QC schema is additionally denser and balances those three fields across the row. Other capacity-oriented categories retain positional accuracy and work envelope. Photos, sources, and verification classifications remain attached to each internal `VendorEquipment` record for future use; the customer route passes only a stripped, opaque `CustomerEquipment` DTO to its client catalog, so suppliers, source documents, provenance URLs, internal notes, and supplier-derived record IDs do not enter the browser bundle. The prior image-led card design is retained in source as `ArchivedEquipmentCardCatalog` for reconsideration. Expanded rows normalize legacy source labels to `Work envelope` and `Positional accuracy (X/Y/Z)`; a single source tolerance is labeled as a common X/Y/Z value rather than fabricated as three independent figures. The HT710 CNC Mill now uses the exact manufacturer-published X/Y/Z positional-accuracy value. The four named Shuofang Swiss/turn-mill lathes use their source-PDF diameter capacities as work envelopes; entries that cannot be matched to an exact model or whose source omits an envelope remain blank rather than inferred. They only link to a technical PDF or online specification when it names the exact machine/model. Supplier identity and source-document provenance remain internal. |

Temporary Sheet Metal scope: only the eight documented laser-cutting and bending machines are customer-visible. Oil-press and riveting records remain in `vendorEquipment` for a future reintroduction.

## Admin Workspace

| Feature | Routes | What It Does | Data Source | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Admin home redirect | `/admin` | Redirects legacy/admin-home traffic to Quote Submissions. | Next.js App Router redirect. | Operational | The standalone admin Overview page is retired; `/admin/quotes` is the active admin home. |
| Admin quote submissions | `/admin/quotes` | Primary RFQ review and quote-issuance command center. Admins inspect RFQs, request more customer information, no-quote unsupported RFQs, attach supplier quote files, select the quoted overseas vendor, enter pricing/lead time, issue customer quotes, and export quote documents. | Request repository, customer quote versions, supplier quote attachments, overseas vendor directory, local upload storage. | Operational | `Request information` sets `NEEDS_INFO`; `No quote` sets `CLOSED`; both require a customer-facing note stored in the existing operator review note field. Issued quotes are read-only by default with explicit edit/reissue mode; legacy `/operator/requests` and `/operator/requests/[requestId]` URLs redirect here. |
| Quote workbook export | `/admin/quotes/[requestId]/quote-template.xlsx` | Generates request-specific customer quote workbook. | RFQ, uploaded files, quote feedback, quote version, shipping fields. | Operational | Dependency-free workbook generator. |
| Admin quote PDF | `/admin/quotes/[requestId]/quote.pdf` | Downloads or previews the latest saved customer quote PDF. | Customer quote PDF renderer and saved quote version. | Operational | DOC-004 Rev 1 is frozen unless a Rev 2 iteration starts. |
| Admin orders list | `/admin/orders` | Shows active placed orders and lets admins archive placed orders. | Purchased request/order data and `Request.isArchived`. | Operational | Archiving does not change `status: PURCHASED`; delivered orders are no longer considered open. |
| Admin order detail | `/admin/orders/[requestId]` | Publishes manual customer-visible order updates through a visual lifecycle, status-aware milestone suggestions, conditional internal/tracking fields, and a live customer preview. | Purchased request/order data, supplier quote/order data, documents, and the shared order-progress fields. | Operational | Customer-facing note is required; active orders require a next milestone/date; recorded status cannot move backward in v1. Supplier PO readiness depends on structured selected shop quote data. |
| Admin invoice PDF | `/admin/orders/[requestId]/invoice.pdf` | Renders the customer invoice PDF from accepted order data. | Same order-backed invoice renderer as customer route. | Operational | Saved immutable invoice issuance is future work. |
| Supplier purchase order PDF | `/admin/orders/[requestId]/supplier-purchase-order.pdf` | Generates DOC-002 supplier PO for the selected Chinese/overseas shop quote. | Accepted order plus structured selected supplier quote line items. | Operational | Does not generate from customer prices or unstructured supplier attachments. |
| Admin customers | `/admin/customers`, `/admin/customers/[companyId]` | Company-grouped customer and user management, plus waiting-list visibility. | Customer/company records, waiting-list data, request summaries, and Prisma customer memberships. | Operational | The profile detail presents Access management first, followed by a collapsed-on-load Business details disclosure with a label-and-field row layout. The Lattice Admin can add users, set Customer Admin/Member roles, reset a password to generate a one-time-displayed temporary credential, request a verified email change, and remove users. An email-change request keeps the current sign-in email active while a 24-hour, single-use confirmation is delivered to the replacement address; verification updates the identity and invalidates the old session. Generated temporary passwords expire after 72 hours and require the user to choose a personal password before normal workspace access. Existing passwords are not readable. The final Customer Admin is protected from removal/demotion until another Customer Admin exists. |
| Admin vendors | `/admin/vendors`, `/admin/vendors/[vendorId]` | Overseas vendor directory and detail records for shop contacts, onboarding, capabilities, quality notes, RFQ history, and order coverage. Seeded vendors include Shenzhen Precision, Dongguan Axis, Tainan Advanced, Jucheng Precision (JC Proto), Best Prototypes, and Zintilon. | Vendor repositories plus local `.data/admin-vendor-overrides.json`. | Prototype | Needs durable supplier/vendor tables for production. |
| Admin material inquiries | `/admin/material-inquiries` | Reviews customer-requested materials that are not yet in the catalog, records internal sourcing notes, and moves each inquiry through New, Reviewing, and Resolved. | Prisma `MaterialInquiry` records with `.data/material-inquiries.json` development fallback. | Operational | Customer submissions originate at `/materials/inquiry`. Production requires the new Prisma model to be applied before rollout. |
| Admin resources | `/admin/resources` | Internal resource library for generated and reference documents. | `resources/admin/`, generated document routes, template metadata. | Operational | Current library includes DOC-002, DOC-003, and DOC-004; DOC-001 is retired. |
| Admin resource document routes | `/admin/resources/domestic-invoice-template`, `/admin/resources/quote-template`, `/admin/resources/supplier-purchase-order-template` | Preview/download internal template documents. | Admin document template renderers. | Operational | Routes require admin authorization. |

## Supplier Workspace

| Feature | Routes | What It Does | Data Source | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Supplier orders | `/supplier/orders` | Reserved supplier-facing order list. | Supplier-accessible purchased order records. | Deferred | Suppliers are not platform users in the current phase; Lattice manages supplier communication internally. |
| Supplier order detail | `/supplier/orders/[requestId]` | Reserved supplier-facing order detail. | Supplier order data and purchased request context. | Deferred | Supplier workflow is intentionally deferred until suppliers are onboarded. |
| Supplier invoice PDF | `/supplier/orders/[requestId]/invoice.pdf` | Reserved supplier invoice access. | Order-backed invoice renderer. | Deferred | Supplier accounts and awarded-shop ownership are not active in the current phase. |

## Shared Workflow And Data Features

| Feature | Main Files/Routes | What It Does | Data Source | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Request persistence | `src/lib/request-repository.ts`, `src/lib/request-persistence.ts`, `src/app/api/requests/route.ts` | Saves and loads RFQs/orders through Prisma, with local fallback in development. | PostgreSQL/Prisma, data-mode selected local fallback. | Operational | Real shared persistence requires Postgres/Neon. |
| Customer/mock data mode | `src/lib/data-mode.ts`, `src/lib/local-request-store.ts`, `src/lib/demo-requests.ts`, `LATTICE_DATA_MODE` | Keeps mock/demo RFQs and customer-safe fallback records in separate runtime spaces. | `customer` mode uses `.data/requests.json`; `mock` mode uses `.data/mock/requests.json` and mature-account demo fixtures spanning RFQ, quote, order, quality, shipment, and delivery states. | Operational | Local development defaults to mock mode. `npm run dev:customer` runs the same localhost app against customer-safe fallback data. Production refuses `LATTICE_DATA_MODE=mock`. |
| Request model and statuses | `src/lib/request-model.ts` | Defines RFQ/order status vocabulary and core request shapes. | TypeScript model plus Prisma schema mapping. | Operational | Buyer lifecycle tags are derived separately for customer-facing language. |
| Buyer lifecycle tags | `src/lib/buyer-lifecycle.ts` | Maps internal request/order states to customer-facing tags: Draft, Quote Requested, Quote Received, In Production, Shipping, Delivered, Archived. | Request and supplier order state. | Operational | `Delivered` is reserved until delivery confirmation exists. |
| Customer activity feed | `src/lib/customer-notifications.ts` | Derives notification rows from RFQ status events, customer quote versions, supplier updates, documents, and shipment state. | Existing request/order records only. | Operational | No activity table or read-state table in V1. Request-info and no-quote rows reuse RFQ status events and display the operator note from `operatorReview.internalNotes`. |
| Customer dashboard summary | `src/lib/customer-dashboard.ts` | Builds dashboard KPIs, grouped action workflows, and quote/order activity rows for quote-received and order-placed events. | Buyer quotes and buyer orders. | Operational | Notification history is intentionally built only by `/notifications`; the old mixed Inbox, Recent Updates preview, and right-side order/contact card have been removed from `/dashboard`. |
| Roadmap interest signals | `src/lib/product-roadmap.ts`, `src/lib/roadmap-interest.ts`, `/roadmap` | Stores customer interest flags for upcoming roadmap items so prioritization can be reviewed. | `RoadmapInterest` Prisma model with local development fallback. | Partial | Current customer view shows aggregate counts; internal prioritization reporting is a future admin surface. |
| Package tracking links | `src/lib/package-tracking.ts`, `src/components/buyer-order-detail.tsx` | Detects common carrier formats from the supplier tracking number and exposes a `Track package` link from order detail. | Existing `supplierOrder.trackingNumber`. | Operational | V1 does not ingest carrier scan events; it links to the carrier or carrier-search page. |
| Local file storage | `src/lib/local-file-storage.ts`, `/api/local-files/[...storageKey]` | Stores and serves local development CAD, drawing, supplier quote, and customer PO files. | Gitignored `.data/uploads` plus `src/lib/request-access-policy.ts`. | Partial | Submitted RFQ files and customer PO files require admin or company-scoped customer ownership. Supplier quote attachments are admin-only. Browser draft files under `rfq-drafts/...` remain customer/admin accessible until draft ownership is persisted. Production needs Cloudflare R2 or another S3-compatible bucket. |
| CAD previews | `src/lib/autodesk-platform-services.ts`, `/api/cad-previews/*`, viewer components | Supports Autodesk Platform Services upload, asynchronous translation, thumbnails, viewer tokens, and a focused native Autodesk toolbar for Measure, Explode Model, and Section Analysis. | APS credentials and stored preview URNs. | Partial | Requires APS env vars and ongoing camera/framing tuning. RFQ configuration and submission should not block on translation completion. |
| Supplier quote files | `/api/supplier-quote-files`, `src/components/supplier-quote-files.tsx` | Lets admins upload received Chinese/overseas shop quote files. | Local upload storage and `SupplierQuoteAttachment` records. | Operational | Production storage hardening still pending. |
| Quote PDF | `src/lib/quote-pdf.ts`, quote PDF routes | Generates customer quote PDFs. | Saved RFQ and quote version data. | Operational | DOC-004 Rev 1 is frozen. |
| Invoice PDF | `src/lib/invoice-pdf.ts`, invoice PDF routes | Generates customer/order invoice PDFs. | Purchased order data and payment/PO context. | Operational | Durable annual issued invoice records exist at schema/repository level but order downloads still render from order-derived references. |
| Supplier PO PDF | `src/lib/purchase-order-pdf.ts`, supplier PO route | Generates supplier-facing purchase order PDF from selected structured shop quote. | Accepted order and selected supplier quote line items. | Operational | Prevents customer-price leakage into supplier PO. |
| Stripe payments | `src/lib/stripe.ts`, `src/lib/stripe-checkout.ts`, `/api/stripe/webhook` | Handles card-only quote checkout and webhook reconciliation. | Stripe PaymentIntent/CardElement and env vars. | Partial | Production requires Stripe secrets, publishable key, webhook secret, and app base URL. |
| Email delivery | Waiting-list, password-recovery, and email-change helpers | Sends or records transactional access emails. | Resend when configured, local outbox fallback. | Partial | Production sender domain and Resend configuration are required for delivery. Email-change verification sends a confirmation to the replacement address and a post-change security notice to the former address. |
| Role and route authorization | `src/lib/session.ts`, `src/lib/workspace-user.ts`, `src/lib/route-authorization.ts`, `src/lib/request-access-policy.ts`, `src/proxy.ts` | Authenticates with Clerk and scopes customer RFQs, quotes, orders, invoices, and submitted files to the signed-in user's customer company. | Clerk authentication/session state plus provisioned Prisma `User`/`Company` memberships, `WorkspaceRole`, and request buyer-company IDs. | Partial | Clerk confirms identity; a matching provisioned Prisma user grants app access and is linked to Clerk on first sign-in. Only the Lattice Admin role reaches `/admin`; Customer Admin and Customer Member roles only access customer routes. The Lattice Admin can deliberately enter a named customer user's company-scoped support view and exit back to admin; this is a signed support session, not a second browser login. Production cutover still requires the Clerk Production instance, Vercel keys, and the `clerkUserId` schema change. |
| Google Workspace SSO | `/api/auth/google/*`, `src/lib/google-sso.ts` | Supports Google OAuth/OIDC login for the configured Lattice Workspace domain, including local-host normalization before OAuth state is created. | Google OAuth web client plus configured local and Vercel Production SSO environment variables. | Partial | Local Google sign-in should start at `localhost:3000`; the route redirects `0.0.0.0:3000` to the configured callback host to retain the secure state cookie. Customer-domain SSO and durable organization membership remain future work. |

## Placeholder Or Future Modules

| Feature | Routes | Intended Direction | Status |
| --- | --- | --- | --- |
| Analytics | `/analytics` | Future metrics/reporting area for operations, buyers, or admins. | Placeholder |
| Projects | `/projects` | Future project/program grouping across RFQs and orders. | Placeholder |
| Durable activity/workflow state | Future schema/table | Persist true read/unread state, explicit workflow-step completion, customer replies, notification preferences, and first-class activity records. | Future |
| Durable supplier/vendor database | Future Prisma models | Promote local vendor overrides and source-trace data into production records. | Future |
| Automated delivery confirmation | Future carrier integration/event | Verify delivered status from carrier events after the manual lifecycle is proven. | Future |

## Maintenance Checklist

When a feature changes, update this document in the same work session if any of these changed:

- Route or navigation label.
- User role or workspace ownership.
- Data source or persistence layer.
- Status level, such as `Prototype` becoming `Operational`.
- Important limitation, security assumption, or production dependency.
- A feature is added, retired, renamed, or moved.

Also update `PROJECT_CONTEXT.md`, `DECISIONS.md`, or `TODO.md` when the change affects product direction, architecture, durable decisions, or next steps.
